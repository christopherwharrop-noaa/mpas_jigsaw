mesh:
  # Coarse (global) grid distance in km
  coarse_resolution: 60.0

  refinements:
    - resolution: 12.0
      # Center of refinement region (degrees)
      center_latitude: 38.0
      center_longitude: -95.0
      # Radius of the uniform refinement region in km
      radius: 2600.0
    - resolution: 3.0
      center_latitude: 38.0
      center_longitude: -95.0
      # Radius of the uniform refinement region in km
      radius: 600.0

